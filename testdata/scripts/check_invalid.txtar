# Test that check fails on invalid config with exit code 2
! exec vanity check --skip-repo-validation
stderr 'domain is required'

-- .vanity.yml --
domain: ""
modules:
  - name: foo
    repo: https://github.com/example/foo
